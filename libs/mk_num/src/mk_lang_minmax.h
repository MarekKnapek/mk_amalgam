#ifndef mk_detail_include_guard_lang_minmax
#define mk_detail_include_guard_lang_minmax


#define mk_lang_min(x, y) (((y) < (x)) ? (y) : (x))
#define mk_lang_max(x, y) (((y) < (x)) ? (x) : (y))


#endif
